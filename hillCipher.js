const readFiles = require('read-multiple-files');
const math = require('mathjs');

// readFiles(['hello.txt', 'world.txt'], 'utf8', (err, contents) => {
//   if (err) {
//     throw err;
//   }
//
//   console.log(contents);
// });

function createKey(n=3) {
  
}

createKey();
